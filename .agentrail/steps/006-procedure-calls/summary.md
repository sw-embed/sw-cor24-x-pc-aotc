Implemented procedure call lowering: call/calln via jal, modified enter to save return address, ret with dynamic return value detection and argument cleanup, loada/storea for argument access, sys 0/1/2 (halt/putc/getc), signed software division helper (_p24_divmod), bitwise ops (and/or/xor/not/shl/shr), indirect memory access (load/store/loadb/storeb/addrl/addrg), and trap. Added nargs extraction to ProcedureInfo. All 51 tests pass. Runtime library procs (write_int, write_ln, write_bool, write_str) now compile through the AOT pipeline.