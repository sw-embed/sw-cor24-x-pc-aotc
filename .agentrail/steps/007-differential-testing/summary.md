Built differential test harness validating AOT output against p-code interpreter. Created shell script (tests/run_diff_tests.sh) and Rust integration test (crates/pcode-aotc/tests/differential.rs). Added globals.pas and nested_calls.pas test programs (8 total). Pipeline: .p24 → pv24t vs .p24 → pc-aotc → .s → cor24-run, compare stdout. All 8 tests correctly detect assembly failures (emitter generates la/lw/sw/mov/ceq which cor24-run doesn't support). Tests skip gracefully when external tools missing. Harness is the quality gate for all future development.